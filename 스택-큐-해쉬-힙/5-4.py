N, M = map(int,input().split())
a = [(key,value) for key,value in enumerate(list(map(int,input().split())))]

res =0
b = a[M]
while True:

    if max(a[1]) == a[0][1]:
        if min(a[0])
    a.pop(0)
    res +=1

    else:
        a.append(a.pop(0))
    
    if b not in a:
        break

